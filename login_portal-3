<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>Library Management</title>

  <style>
    /* 🔹 Main Section */
    .main_section {
      background-image: url('/images_library/library_img2.jpg');
      background-repeat: no-repeat;
      background-position: center;
      background-size: cover;
      min-height: 100vh;
      display: flex;
      flex-direction: column; /* Stack vertically */
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 40px 15px;
      gap: 30px;  
    }

 
    .top-logo img {
      width: 120px;
      height: auto;
      position: relative;
      bottom: 90px;
      opacity: 1;
      transition: transform 0.3s ease, opacity 0.3s ease;
        animation: fadeInUp 1s ease;
    }
    .top-logo img:hover {
      transform: scale(1.05);
      opacity:0.8;
    }

 
    .sub-container {
      background-color: rgba(0, 0, 0, 0.6);
      border-radius: 15px;
      border-bottom: 10px solid rgb(190, 220, 228);
      color: #fff;
      max-width: 800px;
      width: 100%;
      padding: 40px 20px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
      animation: fadeInUp 1.5s ease;
       position: relative;
      bottom: 90px;
    }

 
    .sub-container .btn {
      font-weight: 500;
      letter-spacing: 0.5px;
      transition: all 0.3s ease;
      background-color:#00a651;
       border: 1px solid white;
        color: #ffffff;
      
    
       
    } 

    .sub-container .btn:hover {
      transform: translatey(-3px);
      box-shadow: 0 4px 10px rgba(255, 255, 255, 0.2);
      background-color: #0074c2;
            border: 1px solid white;
             color: #ffffff;
    }

 
    .company-logo {
      width: 300px;
      height: auto;
      border-radius: 10px;
      opacity: 0.5;
      transition: opacity 0.4s ease;
       position: relative;
      bottom: 70px;
        animation: fadeInUp 2.5s ease;
    }

    .company-logo:hover {
      opacity: 0.8;
    }

  
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(70px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

 
    @media (max-width: 768px) {
      .sub-container {
        padding: 25px 15px;
      }
      .top-logo img {
        width: 90px;
      }
      .company-logo {
        width: 200px;
      }
    }

    @media (max-width: 480px) {
      .sub-container {
        border-bottom: 6px solid rgb(190, 220, 228);
      }
      .sub-container h2 {
        font-size: 1.4rem;
      }
      .company-logo {
        width: 150px;
      }
    }
    /* .top-logo{
      animation: appear linear;
      animation-timeline:view();
      animation-range: entry 0;
    }

    @keyframes appear{
      from {
        opacity: 0;
        scale: 0.5;

      }
      to{
        opacity:1;
        scale: 1;
      }
    } */
  </style>
</head>

<body>
  <section class="main_section">

    
    <div class="top-logo" >
      <img src="images_library/tq-logo.png" alt="Top Logo">
    </div>

 
    <div class="sub-container">
        
      <h2 class="fw-bold">Welcome to the Library</h2>
      <p class="mt-3">Select your role to continue:</p>
      <div class="d-flex justify-content-center gap-3 mt-4 flex-wrap">
        <button class="btn   px-4 py-2 rounded-pill">Student</button>
        <button class="btn   px-4 py-2 rounded-pill">Staff</button>
      </div>
    </div>
 
    <img src="images_library/company.png" alt="Company Logo" class="company-logo img-fluid">

  </section>
</body>
</html>
