<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Library - User Selection</title>
<style>
*{
  margin:0;
  padding:0;
  box-sizing:border-box;
  font-family:"Poppins",sans-serif;
}

body{
  background:#fff6f3;
  min-height:100vh;
  display:flex;
  justify-content:center;
  align-items:center;
}

.main-container{
  width:90%;
  max-width:1100px;
  min-height:520px;
  background:#fff;
  border-radius:20px;
  box-shadow:0 6px 25px rgba(0,0,0,0.1);
  display:flex;
  overflow:hidden;
  position:relative;
  animation:fadeIn 0.8s ease;
}
@keyframes fadeIn{
  from{opacity:0;transform:translateY(20px);}
  to{opacity:1;transform:translateY(0);}
}

/* Left orange welcome section */
.left-section{
  flex:1;
  background:linear-gradient(185deg,green);
  color:#fff;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  position:relative;
  overflow:hidden;
}
.left-section::before{
  content:"";
  position:absolute;
  top:-80px;
  left:-80px;
  width:250px;
  height:250px;
  border-radius:50%;
  background:rgba(255,255,255,0.1);
}
.left-section h2{
  font-size:2rem;
  margin-bottom:0.5rem;
}
.left-section p{
  font-size:1rem;
  max-width:250px;
  text-align:center;
}

/* Right selection section */
.right-section{
  flex:1.2;
  background:#fff;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  padding:2rem;
  position:relative;
}

.header{
  font-size:1.8rem;
  font-weight:600;
  background:linear-gradient(90deg,#00a651,#0074c2);
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
  margin-bottom:2rem;
  text-align:center;
}

.selection-box{
  display:flex;
  justify-content:center;
  align-items:center;
  flex-wrap:wrap;
  gap:1.5rem;
  width:100%;
}

.option-box{
  width:220px;
  height:160px;
  background:#ffffffc0;
  border-radius:15px;
  box-shadow:0 4px 10px rgba(0,0,0,0.1);
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  cursor:pointer;
  position:relative;
  transition:all 0.3s ease;
  overflow:hidden;
 
}
.option-box::before{
  content:"";
  position:absolute;
  inset:0;
 
  opacity:0.15;
  transition:opacity 0.3s ease;
}
.option-box:hover{
  transform:translateY( -4px) scale(1 );
}
.option-box:hover::before{
  opacity:0.3;
}
.option-box h3{
  color:#0074c2;
  z-index:1;
  font-size:1.3rem;
  font-weight:600;
}

 
/* .circle{
  position:absolute;
  bottom:-60px;
  right:-60px;
  width:160px;
  height:160px;
  border-radius:50%;
  background:radial-gradient(circle,#00a651 0%,#0074c2 80%);
  opacity:0.4;
  animation:floatCircle 5s ease-in-out infinite alternate;
}
@keyframes floatCircle{
  from{transform:translateY(0) rotate(0deg);}
  to{transform:translateY(-15px) rotate(20deg);}
} */

/* Responsive */
@media(max-width:900px){
  .main-container{  
    flex-direction:column;
    height:auto;
  }
  .left-section{
    width:100%;
    min-height:200px;
    border-radius:20px 20px 0 0;
  }
  .right-section{
    padding:1.5rem;
  }
  .option-box{
    width:90%;
    height:140px;
  }
  .header{font-size:1.5rem;}
}
</style>
</head>
<body>
  <div class="main-container">
    <!-- LEFT SIDE -->
    <div class="left-section">
      <h2>Welcome</h2>
      <p>You are 30 seconds away from accessing the Library!</p>
    </div>

    <!-- RIGHT SIDE -->
    <div class="right-section">
      <h2 class="header">Library Management</h2>
      <div class="selection-box">
        <div class="option-box" onclick="navigateTo('student')">
          <h3>Student</h3>
        </div>
        <div class="option-box" onclick="navigateTo('staff')">
          <h3>Staff</h3>
        </div>
      </div>
      <div class="circle"></div>
    </div>
  </div>

<script>
function navigateTo(type){
  // Navigate to your register or next page
  window.location.href="library_management.aspx?user="+type;
}
</script>
</body>
</html>
